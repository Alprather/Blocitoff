<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%=  item.name %>
      <%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok' %>
    </h4>
    <small>
      Time remaining: <%= distance_of_time_in_words(Time.now, item.created_at + 7.days) %>
    </small>
  </div>
</div>
