<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to item.name, user_items_path(item.user, item) %>
    </h4>
    <small>
      submitted <%= time_ago_in_words(item.created_at) %> ago
    </small>
  </div>
</div>
