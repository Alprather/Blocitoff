<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%=  item.name %>
      <%= link_to "", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok' %>
    </h4>
    <small>
      submitted <%= time_ago_in_words(item.created_at) %> ago
    </small>
  </div>
</div>
